const num = [1, 3, 4, 5];
const final = num.splice(2, 2); // tira por condição a primeira, aparti de qual index 2º aparti do index que indiquei quantos vou tirar
const final2 = num.shift(); //tira o primeiro
const final3 = num.pop(); //tira o ultimo
console.log(num);
