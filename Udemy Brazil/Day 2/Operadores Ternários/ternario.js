let driver = 90;
let speed = driver > 110 ? 'Above' : 'Below';

// O operador ternário é usado para atribuir um valor com base em uma condição.
// Se a condição (driver > 110) for verdadeira, a variável speed recebe 'Above', caso contrário, recebe 'Below'.
console.log(speed);
